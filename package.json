{
  "name": "songfind",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm run start:dev --workspace=@songfind/api",
    "dev:web": "npm run dev --workspace=@songfind/web",
    "build:api": "npm run build --workspace=@songfind/api",
    "build:web": "npm run build --workspace=@songfind/web"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  }
}
